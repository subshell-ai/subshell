---
"@internal/desktop-server": patch
---

The supervision and setup screens name no service managers any more: the background option states what is true ("The server runs on this machine, not in this app. If it stops, it is started again."), the switch reads "Start the server at login", and its help states the CURRENT state rather than a static warning. Same words the client now uses.
