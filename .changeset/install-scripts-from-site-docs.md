---
"@internal/docs": patch
---

The server install pages now print `curl -fsSL https://subshell.sh/install-server.sh | bash`, matching the website and README; the node enrollment one-liner (served by your own server) is unchanged.
