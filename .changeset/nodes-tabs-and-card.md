---
"@internal/server": patch
---

The Nodes page is now grouped into a left-hugging tab set that speaks the same visual language as the subshells list — Tiles / List / Keys with the shared icons, where the card list, a new column-aligned table of the same machines, and the setup-key ledger each get their own address (`/nodes`, `?tab=list`, `?tab=keys`). The page subtitle reads "Nodes allow you to run subshells on other machines." On the node harness card, the Install button moved up onto the harness's own row (shrunk to just "Install"), replacing the per-row "checked … ago" stamp that used to occupy that cell.
