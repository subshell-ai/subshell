---
"@internal/desktop-server": minor
---

First run now provisions itself: opening the app on a clean machine installs the bundled server, service and start with no button press — the only stop is tmux when it is missing. An available app update is now visible where it wasn't: the tray item names it and opens the update screen, and the update screen gains **Later**. The About facts move to the standard app-menu panel. (spec 2026-09-17-zero-touch-desktop-setup)
