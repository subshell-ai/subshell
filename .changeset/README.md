# Changesets

Run `bunx changeset` after user-visible changes to `@internal/server` or
`@internal/client` — the version PR on merge to main records the bump; the
release cut happens via `.github/workflows/release.yml`.
