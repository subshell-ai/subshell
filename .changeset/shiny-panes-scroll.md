---
"@internal/server": patch
---

Server bundle carries the iPad terminal fixes: two-finger touch pan and Magic Keyboard trackpad (wheel) now scroll the terminal instead of the PWA shell; grid gestures are locked against native scroll; Preferences shows the bundle build id.
