---
"@internal/docs": minor
---

Documentation rewritten in a neutral vendor-docs voice (STYLE.md, modeled on the Tailscale and NetBird docs): plain task headings, numbered steps with result sentences, rules and exceptions separated. Page set, URLs, and every factual claim are unchanged; stale claims corrected against code along the way (protocol-14 link encryption, four release triples, no docs CI gate, real UI labels).
