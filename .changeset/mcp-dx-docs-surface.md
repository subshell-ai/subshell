---
"@internal/docs": minor
---

The docs meet the MCP DX wave. `mcp-tools` is the fifteen-tool reference now: `list_nodes`, `read_subshell_log` and `send_to_subshell` documented, the `node` and `prompt` arguments covered, and the mcp-server, rest-api, architecture and channels pages updated to match the agent surface.
