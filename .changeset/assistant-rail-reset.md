---
"@internal/desktop-server": minor
---

The assistant's rail gains Reset as its fifth, destructive-styled section: the door moves into the sidebar while the reset screen stays full-window, so nothing competes with the chain running. With the rail present it is the navigation — the standing screens' leave buttons (Back on How it runs, Back on Addresses, Close on Update) render only where the rail does not, keeping the way out for screens that have none.
