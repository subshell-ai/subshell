---
"@internal/server": minor
---

Server Settings → Networking now owns the Addresses card (moved from Service: port, bind, base URL and trusted origins are the page's own question), and the installed network plugins are grouped into one Networks card instead of a card each.
