---
"@internal/docs": minor
---

Networking & Addresses and the mobile page follow the LAN-address derivation: the allowlist now has four sources, the phone-on-the-Wi-Fi case is configured by nothing, and the `403 "Invalid origin"` page speaks about names — the spellings that still need a trusted-origin entry.
