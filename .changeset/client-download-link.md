---
"@internal/server": patch
---

The Add-node dialog's **Desktop App** path now says where to GET Subshell Client. It showed the two values the Enroll step takes and nothing else — which left a reader without the app at a dead end, since the desktop bundles are served by nobody but the project's GitHub Releases. One detail line under the rows links to the release list filtered to `desktop-client` (never `/releases/latest`: four components share this repo and "latest" is whichever was tagged last).
