---
"@internal/server": patch
---

Networking cards read as one system: the join options sit inside one bordered group with the pills hugging their labels, the address rows take the same quiet label-over-value grammar as the other facts on the card, and the design system now names the two label grammars (quiet for read-only data, bold for a control's label and a section heading).
