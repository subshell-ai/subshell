---
"@internal/desktop-server": patch
---

The macOS permissions screen now lists its rows actionable-first (Notifications, Photos, Files and Folders), and the Files and Folders row is honest about being explanation: it carries no pending ring it could never tick. Its System Settings button appears on every arrival except the first-run handoff, where a machine macOS has never asked would open an empty pane; the picker's "Blocked by macOS" notice, whose refusal is what puts the toggle in the pane, always keeps it.
