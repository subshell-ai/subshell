---
"@internal/desktop-server": patch
---

The macOS permissions screen now lists its rows actionable-first (Notifications, Photos, Files and Folders), and the Files and Folders row is honest about being explanation: it carries no pending ring it could never tick, and its System Settings button appears only when the dashboard's "Blocked by macOS" notice sent you here, because that door guarantees the pane holds the toggle you came to flip. On a first run the pane is guaranteed empty, so the row just says when macOS will ask.
