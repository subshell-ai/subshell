.expo/
# Local instance credentials for scripts/m1-transport-harness.ts (Bun auto-loads
# .env*). Never commit a login — the token these produce can type into any pane.
.env
.env.*
dist/
build/
*.log
expo-env.d.ts

# Generated by `expo prebuild` / `expo run:*` (Continuous Native Generation).
# Never hand-edited and never committed — app.json is the source of truth.
/android/
/ios/
