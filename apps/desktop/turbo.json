{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "clean": {},
    "lint": { "inputs": ["src/**", "*.json"] },
    "lint:check": { "inputs": ["src/**", "*.json"] },
    "test": { "cache": false },
    "verify-types": { "inputs": ["src/**", "*.json"] }
  }
}
