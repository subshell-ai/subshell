{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "console",
  "description": "The bundled server-control page. It is the ONLY surface allowed to drive the subshell-server CLI, which is why it is a separate window from the one showing the server's own SPA.",
  "windows": ["console"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-open",
    "dialog:allow-message",
    "opener:allow-open-url",
    "opener:allow-reveal-item-in-dir"
  ]
}
