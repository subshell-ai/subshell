import { Text, View } from "react-native";

/** Placeholder for the Settings tab (lands with task 11 of the app-shell plan). */
export default function SettingsTabPlaceholder() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ color: "#8b9095" }}>Settings — coming in the next build</Text>
    </View>
  );
}
