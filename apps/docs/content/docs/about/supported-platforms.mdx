---
title: Supported Platforms
description: Which operating systems and architectures can run the control plane, the node agent, and the desktop apps.
---

Subshell ships as prebuilt binaries — no runtime to install beside them, no package manager in the picture. Three questions answer most of what people have: does my OS run the binaries, does my machine need tmux, and what about my phone.

## The server-side binaries

The control plane (`subshell-server`, with the web UI embedded in it) and the node agent (`subshell`) are each published as one self-contained binary per architecture:

| Platform | Control plane | Node agent |
|---|---|---|
| Linux x86_64 (`linux-x64`) | ✓ | ✓ |
| Linux arm64 (`linux-arm64`) | ✓ | ✓ |
| macOS, Apple silicon (`darwin-arm64`) | ✓ | ✓ |
| macOS, Intel | — | — |

No Bun or Node is needed on a host running these — they carry their runtime. Intel Macs are not built for any component, so an x86_64 Mac is out.

### The Linux floor

Every Linux artifact is built on Ubuntu 24.04, which sets a **glibc 2.39 floor**: the binaries need Ubuntu 24.04 or Debian 13 (or a comparable newer distro). Ubuntu 22.04 and Debian 12 will not run them — not just the GUIs, any of the components. This is a build choice that is now the published contract, not an untested hunch; older distros need a from-source build on their own toolchain.

## The desktop apps

The two GUIs — Subshell Server (installs and manages a control plane on this machine) and Subshell Client (your interface to a control plane, and where this machine enrolls as a node) — are Tauri apps covering a narrower set:

| App | macOS 13+ (Apple silicon) | Linux x86_64 |
|---|---|---|
| Subshell Server | signed, notarized disk image | `.deb` (Ubuntu 24.04+ / Debian 13+) |
| Subshell Client | signed, notarized disk image | `.deb` (same floor) |

There are no desktop builds for Intel Macs or arm64 Linux, and both apps need the same glibc floor on Linux. Each app bundles the CLI binary it manages, so a desktop install needs no downloads on first run. [Installing the Server](/get-started/install-server) covers which artifact to reach for.

## Universal requirements

- **tmux 3.2 or newer on every machine that runs a pane.** That is the control-plane host if it runs agents itself, and every [node](/nodes/add-node). A machine that only hosts the UI needs nothing but the binary. Where tmux is missing, the desktop app's setup assistant can install it with the platform's own package manager.
- **An agent CLI where you want agents** — `claude`, `opencode`, `codex`, or whichever [harness plugin](/agents/installing-agent-clis) you launch. The terminal plugin runs a plain shell, so a bare machine is still launchable without any agent installed.
- **Bun 1.4+ only if you run from source.** The prebuilt binaries are self-contained; the from-source workflows in this repo (`bun run start`, tests, building a release) are what need the runtime.

One version detail worth knowing: tmux 3.6 additionally reports a crashed pane's exit code, which the UI shows; on 3.2–3.5 that one read degrades to "unknown", never to a wrong answer.

## Browsers and mobile

The web UI is an xterm.js terminal application: use a current browser, and everything works. There is no per-browser support matrix — if the terminal misbehaves in yours, that is a bug worth reporting. Below a 1024-pixel viewport the UI switches to its small-screen shell — drawer navigation, tabbed workspaces, an on-screen terminal key bar — and it installs from the browser via Add to Home Screen (there is no offline service worker; the pane always comes from the server).

The [native mobile companion](/get-started/mobile) exists today as the fourth surface for the things a web page cannot do — background push, app-icon badge, lock-screen actions, and a Keychain-held credential — and it adds a live terminal tab of its own. Be aware of what it is *not* yet: there is no app-store listing to point at. It ships built from the repository, so today it is for people comfortable running an Expo build, while the browser shell above serves everyone else.

Docker is another way to run the control plane — a compose recipe exists and binds to loopback by default; see [Docker](/server/docker).

## See also

- [Install the Server](/get-started/install-server) — download, verify, run
- [Add a Node](/nodes/add-node) — the agent on another machine
- [Mobile](/get-started/mobile) — the companion app in detail
