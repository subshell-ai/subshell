---
title: Registration and enrollment
description: "The two self-service doors: who may create an account, and who may add a machine."
---

Control who may create an account on this instance and who may add a machine to it.

Your instance has two self-service doors: sign-up, which creates people, and node enrollment, which creates machines. Sign-up has more than one way in, the E-mail door and any external [Sign-in providers](/server/sign-in-providers) you have added, but the door's meaning is the same whichever one you turn. Both doors have switches, and both switches share one semantic worth reading before you flip either: turning a door off stops new traffic, and it does not recall anyone already through. Closing sign-up signs nobody out. Turning off enrollment leaves every already-minted setup key working until it expires or you delete it.

## Registration

Registration is closed by default. The one exception is the instance's first moment: while the instance has no users at all, the door stands open. The first registered account becomes the admin, and a closed empty instance could never create the one person able to open it. The door is open exactly until the first account registers, and closes behind them. Afterwards, an admin decides. The switch lives on the E-mail provider's row under Settings → Auth (E-mail is on the providers list like everything else), and reopening sign-up is an audited act. A scripted session that opened it to mint a throwaway account used to leave no trace; it no longer can.

One function answers whether the instance accepts a sign-up, and three surfaces read their answer from it: the check that refuses the request itself, the admin's switch, and the sign-in page's "create an account" link. That is deliberate: three independent readings is how a page ends up saying "Open" on an instance that refuses every sign-up. The function also fails closed: anything the E-mail row stores other than an explicit yes reads as closed. Treating corruption as "open" would silently re-open a locked-down instance.

External providers answer the same question for themselves: each provider row on Settings → Auth carries its own allow-account-creation switch, independent of the E-mail row's. A provider can also put every new account it creates onto an approval queue. That half of the story is on [Sign-in providers](/server/sign-in-providers).

### Passkeys

A passkey is an additional credential per user and device. It mints the same session cookie a password does. It is not a second factor, and nothing enforces its use; users add them on their own Account page.

One deployment fact to know before you rely on them: passkeys are bound to the host in `APP_BASE_URL`, not to whatever address you happen to be browsing on. Reach the same instance by another name (the loopback spelling instead of the domain, say) and passkeys fail in the browser. If you move `APP_BASE_URL`, passkeys move with it, and stop working on the old address.

### Break-glass recovery

If your last admin forgets their password, the recovery hatch is an environment variable. While `SUBSHELL_EMERGENCY_PASSWORD` is set in the environment the server starts with, an admin signing in with that exact value gets a real session, and their stored credential is overwritten by it. This is a forced password reset, not a bypass: it is destructive by design. While it is armed, every signed-in user sees a warning banner (the flag is public on purpose: the banner is the point), the rewrite is audited as `emergency_login.rewrite_credential`, and the comparison is constant-time and shares the ordinary login backoff.

> [!warning] Clear the variable after recovery
> Clear `SUBSHELL_EMERGENCY_PASSWORD` and restart once you are done. While it is set, anyone who can read the server process's environment can become any admin.

## Node enrollment

By default, any signed-in user may mint a node setup key and add a machine to the instance. That is the behavior of an instance that never touched the setting. The `allow_node_enrollment` switch (Settings → General, admin, audited) narrows minting to admins; a non-admin is refused with a message telling them to ask one. Admins are never affected by it.

As noted at the top of this page, switching it off means "stop handing these out", not "revoke what is outstanding". A minted key stays usable until it is consumed, until it expires (they are single-use and expire in 24 hours), or until someone deletes it on the Nodes page. Deletion is the act that revokes, and it is audited. What a setup key grants is exactly the right to register one machine; see [Add a node](/nodes/add-a-node).

## See also

- [Sign-in providers](/server/sign-in-providers): the other people-doors, and the approval queue
- [Users and roles](/server/users): who an admin is and what the role can do
- [Add a node](/nodes/add-a-node): the enrollment flow a setup key opens
- [Audit log](/server/audit-log): where each flip of each switch is recorded
- [Security model](/concepts/security-model): the trusted-network posture these doors assume
- [The full threat model](https://github.com/subshell-ai/subshell/blob/main/docs/security.md#2-identity-and-credentials): the gate's fail-closed rules and the passkey and break-glass accounting, with enforcement points
