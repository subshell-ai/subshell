---
title: Reset
description: "Tear the instance down from its own machine: what a reset deletes, what it never touches, and the confirmation that gates it."
---

Delete everything an instance has ever written, from its own machine, by the guarded assistant path or by hand.

Reset is the one act that deletes everything the instance has ever written, so it is deliberately kept off the network: no page you serve can drive it. The doors to it all stand on the machine itself: the Subshell Server desktop assistant, its tray menu, and `subshell-server reset` on the CLI. They run one chain and ask one confirmation, the machine's name. (The old ruling that the CLI would never carry a reset verb was reversed on 2026-09-26, issue #232: what stayed was the no-network rule and the typed name, not the hand work.)

## From the desktop app

The dashboard's danger card on Settings is only a door: it raises the Subshell Server app's assistant to the reset screen. The remote page can name a screen, never a path, and never a command. Everything that follows happens on the bundled assistant page, on this machine. A machine that never finished setup has no dashboard to stand on, which is why the app's tray menu carries **Reset…** too: it raises the same confirmation over a first-run screen, the door a wrong-machine install reaches for (issue #232).

**What the screen shows before you can arm it:** every path the reset will delete. They are not a template. The assistant reads them from the server's own `status` report at that moment, the same list [Files and paths](/reference/files-and-paths) describes: the database file, the pane logs, the node artifacts, the data directory, and the config home with its `config.env`. The report is taken all-or-nothing: a partial one refuses to arm at all, because deleting a subset and reporting success is worse than deleting nothing. The plan is held by the app for the whole chain: the sequence stops and uninstalls the very server whose report named the paths, so it cannot be re-asked afterward.

**The confirmation is typing this machine's hostname**, the same one the screen displayed, compared on the machine itself. Then two guards run before anything is deleted: every target must be an absolute path that is neither `/` nor your home directory, and any recursive delete that is or contains the installed server binary is refused outright. The binary at `~/.local/bin/subshell-server` survives; the config file does not, since it is one of the things deleted.

**What the chain does,** step by step with the screen showing its place: stop the server, close the pane servers (each local subshell's tmux), uninstall the service definition, delete the paths, clear the Subshell Server app's own settings, then restart the app. The restarted app re-probes the machine and finds it at first run, ready to set up again.

**What no reset reaches:**

- **Enrolled remote nodes.** Each keeps its node key and keeps running. It stops receiving commands, because the control plane it dialed is gone.
- **A same-machine node.** The `subshell` daemon and its config belong to the node role, a different program with its own config home, and are untouched. Stop it yourself from Subshell Client, or with `subshell service stop` on that machine. Subshells running on other machines are not shut down by resetting this one; they live on their nodes' disks and die with their own panes.
- **The installed binary**, by the containment guard above, and the Subshell Client app's own choices.

> [!caution] Backups go too
> Database backups are kept inside the data directory, which makes them covered by design: a reset leaves no copy behind. If anything in the database is worth keeping, run `subshell-server backup` and move the file somewhere else first.

## From the CLI

`subshell-server reset` runs the assistant's chain without a window: it stops the service, waits for its port to go quiet, closes the pane servers, removes the service definition, and deletes exactly the paths `status --json` names, config home last. The binary stays, so `subshell-server init` can set the machine up again. `subshell-server uninstall` is that chain plus the installed binary and any `.previous` a prior update left; a source checkout is named and left alone.

A server started by hand has no service to stop; the verbs ask the port, and if it still answers they say so by name and clear the machine anyway, ending with a failing exit code. Nothing that survives consent spares the files: a step that cannot run is reported, and a reset means it all goes. Local pane servers go with the plane (remote nodes' panes live on their nodes' machines and are untouched), and the same-machine `subshell` agent keeps its own home.

```bash
subshell-server reset                              # prompts: type this machine's name
subshell-server reset --confirm "$(hostname)"     # the scripted spelling of the same name
subshell-server uninstall --confirm "$(hostname)" # and take the binary
```

The confirmation is the machine's name, compared on the machine. `--yes`, which answers every other question the server CLI asks, is refused by name here: a flag a rushed operator reaches for that silently did not apply must say so. Without a terminal to type into and without `--confirm`, both verbs refuse and change nothing before they touch a manager or a file.

What remains after any reset is on the other machines: nodes still hold their keys, and there is no control plane left to call. You remove them on each machine with the [node CLI](/reference/node-cli); nothing on this host can reach them.

## Other pages

- [Service and autostart](/server/service): what `service uninstall` stops, and the pane-safety rule around it
- [Backups](/server/backups): the snapshot you should carry off-box first
- [Files and paths](/reference/files-and-paths): every path the report names, and what lives in it
- [The threat model](https://github.com/subshell-ai/subshell/blob/main/docs/security.md): why destructive acts stay local on this posture
