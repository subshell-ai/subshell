---
title: Update a node
description: "Move an enrolled node to a newer binary from the page or the machine, and how rollback works there."
---

Move an enrolled node to a newer binary, from the page or from the machine's own keyboard, with a transaction that puts the old binary back when the new one fails.

A node replaces its own binary and restarts into the new one. Both routes run one implementation. An update is a restart with a file swap in front of it, so it carries the restart's permissions and refusals, plus one addition: a safety transaction that puts the old binary back automatically when the new one fails.

## Check for a newer release

To ask the machine what is available:

```bash
subshell update --check
```

It names the newest release the release source publishes that the publisher signed. An unsigned or unverifiable release is refused by name and never offered. One thing `--check` cannot tell you is whether your plane can talk to that version: the binary holds no REST credential, so it cannot ask. The control plane knows. The Components table under Settings → Updates ([Update the server](/server/updating)) lists every node's version against what it offers, and a node's own page says in plain words when the installed binary is too old or speaks a protocol the plane does not. [Version Compatibility](/reference/versions) explains the version floors, and `--to <version>` is how you act on an answer you read there.

## Updating from the machine

```bash
subshell update            # move to the newest release
subshell update --to 0.9.0 --yes
subshell update --from ./subshell-node-cli-linux-x64 --yes
```

`--from` installs a local file instead of downloading. That is the path for an air-gapped fleet: with `SUBSHELL_RELEASE_URL` set empty, every networked update refuses and names `--from` instead of failing quietly. `--force` allows a downgrade.

The two routes differ in when they refuse. The page-commanded update refuses before downloading anything when the restart it will request cannot mean what it says, which is an unsupervised node, or a service definition that would close live panes without `force`. That ordering matters because a refusal there arrives before 70 MB has crossed the wire. The CLI at the keyboard installs first, and if the manager cannot or should not be restarted, it reports that plainly with the new binary already in place.

Before the CLI moves the binary, it checks what the machine can answer locally: no downgrade without `--force`, and an installed binary that can actually be resolved. It also checks the release's publisher. `release-manifest.json` and its signature are two small reads, verified against a key compiled into this binary, and the artifact is downloaded only after they pass. The digest the downloaded bytes are hashed against comes from that signed manifest, never from the `.sha256` sidecar. Any candidate, downloaded or copied with `--from`, must then run and report the exact version it claims.

## Updating from the page

The **Nodes** section of the Components table under Settings → Updates offers the same act remotely. The gate is owner or `edit` grantee, browser session only, and the control plane host's own row is never offered; the act is audited as a node update. The version it offers is the newest your plane can actually talk to, not simply the newest published.

What crosses the wire is a signed `update` command naming a download URL, a digest, and the release's signed manifest with its signature. The node verifies that signature against its own compiled-in publisher key before it replaces anything, so the plane's digest is a second check, not the trust anchor. Even a control plane persuaded, or compromised, after composing the command cannot get the node to install bytes the publisher did not sign. The plane can order an update; only the publisher's key can authorize what runs. A node too old to verify the manifest would silently ignore it and install on the old rule, so your plane refuses to send one an update at all, and the node's page says so.

The URL embeds a one-time download token. It is held in memory, single-use, valid for ten minutes, bound to that one node and one platform, and refused on the checksum routes. It is not a credential in any general sense. The node key still does nothing on the REST API, and this token buys exactly one download of a file the release source publishes publicly anyway.

Two consequences:

- The download URL is built from the plane's configured address, so an instance configured with a loopback base URL will tell remote nodes to fetch from `127.0.0.1`. That is the enrollment trap in a new place: if remote updates fail at the download step on an instance whose base URL is loopback, this is the reason.
- A plane restart forgets outstanding tokens. The node's download then fails, and the update is reported as failed, which is the accurate report, not a silent retry.

## The update is a transaction the new binary completes

Whoever swaps the files cannot see whether the new binary works; the next boot can. So the updater writes a pending marker, keeps the old binary as `<binary>.previous`, and swaps. The node that boots next settles it:

- The plane accepts the node (the new binary connects and is treated as a working node), and the marker and the `.previous` file are deleted. The update is over.
- The plane refuses the node (wrong version, wrong protocol), and the marker says the refusal followed an update. The node renames `.previous` back, records the failure, and exits so the service manager respawns the version that worked, on a machine nobody visited. That swap-back is the node's entire rollback. A node has no database, so unlike [Update the server](/server/updating) there is no data half to restore.
- A refusal with no marker is the ordinary "your node is too old" case, and the node does not roll back for an update that never happened.

At any point afterwards, `subshell update --rollback` puts the kept `.previous` binary back, if one exists.

## What an update actually trusts

Both ends download code from the release source, and neither side has to trust it. Every release carries `release-manifest.json` plus a detached minisign signature over its exact bytes (`release-manifest.json.sig`), made with the publisher keypair. Every path that installs a release (this machine's `subshell update`, the page-commanded update, and the server's own) verifies that signature against a public key compiled into the product, and takes the install digest from the signed manifest's `assets` map, never from the `.sha256` sidecar. The release source still chooses which signed release you get. It can withhold updates or replay any release the publisher ever signed, but it cannot choose what you run. Authenticity is the publisher's key; TLS is only delivery.

Two accounted exceptions:

- The install one-liners still verify against the sidecar, because the machine they are building has no publisher key yet. That stays sound because the server refuses to fetch or cache a node artifact whose manifest does not verify, so the bytes the script digests were signed before they were ever served. Artifacts you published into the downloads directory yourself are the exception: they sit outside the signed feed, are digest-verified only, and were put there by this machine's own operator.
- `subshell update --from <path>` is signature-free: a file you named is your decision.

If your `SUBSHELL_RELEASE_URL` points somewhere else, you have moved delivery, not trust. Setting it empty disables every update and fetch path by name. That is the air-gapped configuration, where `--from` and hand-installed binaries are the whole story.

## Other pages

- [Update the server](/server/updating): the same transaction with a database rollback added.
- [Version Compatibility](/reference/versions): protocol versions and minimum node versions.
- [Manage a node](/nodes/manage): the service controls an update rides on.
- [The updates accounting](https://github.com/subshell-ai/subshell/blob/main/docs/security.md#1112-updates): what replacing an executable costs.
