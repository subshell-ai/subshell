# Applies to every `bun test` in this package. The preload points SUBSHELL_CONFIG_HOME
# at a throwaway directory (and skips the tmux preflight) so the suites never
# touch the developer's real ~/.config/subshell and never require a tmux server.
[test]
preload = ["./src/test-preload.ts"]
