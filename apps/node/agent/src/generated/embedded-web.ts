// GENERATED by src/scripts/embed-web.ts — do not edit
// TRACKED STUB — the committed state of this module. At release time
// `bun run src/scripts/embed-web.ts` OVERWRITES this file with EMBEDDED = true
// and the base64 of every file in apps/node/web/dist; the release script
// restores this stub with `git checkout` after publishing.
//
// It is deliberately TRACKED, not gitignored: `dashboard/web-static.ts`
// imports it unconditionally (project rule: no dynamic imports), so the import
// must resolve on a fresh clone for `verify-types` and `tsc` to pass. With
// this stub in place EMBEDDED is false and the map is empty, so a dev-compiled
// binary serves the dashboard from `apps/node/web/dist` on disk, or the
// no-dashboard notice when even that is absent — the same ladder the server's
// static plugin runs (spec 2026-09-03 §4, ported for the node).

export const EMBEDDED = false;

export const EMBEDDED_WEB: Record<string, string> = {};
