// GENERATED by scripts/embed-web.ts — do not edit
// TRACKED STUB — the committed state of this module. At release time
// `bun run src/scripts/embed-web.ts` OVERWRITES this file with EMBEDDED = true
// and the base64 of every file in apps/frontend/dist (spec 2026-09-03 §4);
// the release script restores this stub with `git checkout` after publishing.
//
// It is deliberately TRACKED, not gitignored: the static plugin imports it
// unconditionally (project rule: no dynamic imports), so the import must
// resolve on a fresh clone for `verify-types` and `tsc` to pass. With this
// stub in place EMBEDDED is false and the map is empty, so boot serves the
// frontend from disk exactly as before embedding existed.

export const EMBEDDED = false;

export const EMBEDDED_WEB: Record<string, string> = {};
