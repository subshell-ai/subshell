# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# host-specific compose additions (auto-merged by `docker compose`)
docker-compose.override.yaml
# personal git identity for the container (template: docker/gitconfig.example)
docker/gitconfig

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Fumadocs generated source (apps/docs: the MDX virtual module)
.source
# Next.js generated type declarations (apps/docs; regenerated by dev/build)
next-env.d.ts

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/
# Comment in the public line in if your project uses Gatsby and not Next.js
# https://nextjs.org/blog/next-9-1#public-directory-support
# public

# vuepress build output
.vuepress/dist

# vuepress v2.x temp and cache directory
.temp
.cache

# Docusaurus cache and generated files
.docusaurus

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# yarn v2
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*
.idea/
.turbo/
.hashes.json

# Subshell runtime data (SQLite DB + session logs)
data/
apps/server/api/data/
apps/server/api/.bun/

# release drop dirs: `release:cli-server` output + CI build staging (binaries, never committable)
dist-server/
dist-rel/

# Claude Code worktrees + subagent-driven-development scratch
.claude/worktrees/
.superpowers/

# stray SQLite file created when a `:memory:` URI is opened as a path by backend tests
file::memory:*

# e2e scratch (Playwright)
e2e/.auth/
e2e/test-results/
e2e/playwright-report/

# macOS Finder metadata — never useful in a diff.
.DS_Store

# The desktop apps: the staged sidecar is a ~100 MB build input, never a commit.
# Spelled per app rather than as `apps/*/desktop/...` on purpose — a glob here
# would also swallow anything a future desktop app adds, silently.
apps/server/desktop/src-tauri/binaries/*
!apps/server/desktop/src-tauri/binaries/.gitkeep
apps/server/desktop/src-tauri/target/
apps/server/desktop/src-tauri/gen/
apps/client/desktop/src-tauri/binaries/*
!apps/client/desktop/src-tauri/binaries/.gitkeep
apps/client/desktop/src-tauri/target/
apps/client/desktop/src-tauri/gen/
# The Vite build of the two desktop apps' windows (their `frontendDist`s).
# Spelled out even though the blanket `dist` rule above already covers them,
# so deleting that rule does not silently start committing a build output.
apps/client/desktop/ui/dist/
apps/server/desktop/ui/dist/
crates/desktop-core/target/
